# Server Port
PORT=3001

# Environment
NODE_ENV=development

# PostgreSQL Configuration
# Individual environment variables or DATABASE_URL
PGHOST=localhost
PGPORT=5432
PGDATABASE=f
PGUSER=postgres
PGPASSWORD=your-secure-password

# Ollama LLM Server Configuration
# Used for code analysis and problem translation on the backend
# Leave empty to disable AI features
OLLAMA_URL=http://localhost:11434
OLLAMA_MODEL=qwen2.5-coder:3b

# Exchange Key for Server-Only Routes
# Used to secure internal API endpoints (analysis-cache, ollama proxy)
# Must match NEXT_PUBLIC_EXCHANGE_KEY in frontend .env
# Generate a strong random key: openssl rand -base64 32
EXCHANGE_KEY=your-exchange-key-change-this-in-production

# Judge Sandbox Configuration (Optional)
# Container runtime for code execution: docker or podman
JUDGE_RUNTIME=docker

# Logging
LOG_LEVEL=info